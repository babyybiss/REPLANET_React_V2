function ExchangeInfo(){
    

    if(exchange.status === "대기"){
        return
    } else if (exchange.status === "승인"){

    } else if (exchange.status === "완료") {

    }
}

export default ExchangeInfo;