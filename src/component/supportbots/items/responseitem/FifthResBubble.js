import { useDispatch, useSelector } from "react-redux";

function FifthResBubble({questionCode}) {
    return (
        <div className="response-box">
            {questionCode}
        </div>
    );
}
export default FifthResBubble;